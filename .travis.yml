language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t dockerzent/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dockerzent/docker-react npm run test